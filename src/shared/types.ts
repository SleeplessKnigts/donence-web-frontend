export interface RecyclePoint {
    recyclePointDetail: string;
    lat: number;
    lng: number;
}

export interface UserInfo{
    email: string,
    imageUrl: string,
    latitude: number,
    longitude: number,
    fname: string,
}